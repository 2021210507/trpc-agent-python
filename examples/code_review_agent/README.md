# Automatic Code Review Agent

Implementation follows the task order and acceptance criteria in the repository
root `DEV_SPEC.md`. User documentation is completed in schedule task E2.

`evaluate.py --sandbox local` is an offline public-proxy gate: its corpus can
support AC2 evidence, but it does not prove results on the official hidden set.
